name: Einmalige Auswerten-Testkorrektur

on:
  push:
    branches:
      - release/v2.25.2-auswerten-aufloesen

permissions:
  contents: write

jobs:
  korrigieren:
    if: ${{ !contains(github.event.head_commit.message, '[auswerten-tests-korrigiert]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release/v2.25.2-auswerten-aufloesen
          fetch-depth: 0
      - name: Veraltete Auswerten-Tests korrigieren
        run: python3 scripts/agent_fix_auswerten_tests.py
      - name: Korrektur committen
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Auswerten-Tests auf Termvertrag aktualisieren [auswerten-tests-korrigiert]"
          git push origin HEAD:release/v2.25.2-auswerten-aufloesen
