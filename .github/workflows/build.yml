name: Android-Build

on:
  push:
  pull_request:

jobs:
  pruefen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Android SDK 36 installieren
        run: sdkmanager "platforms;android-36" "build-tools;35.0.0"
      - name: Architektur prüfen
        run: python3 scripts/pruefe_architektur.py
      - name: Bauen und testen
        run: ./gradlew --stacktrace test :app:assembleDebug
      - name: Quellbaum verpacken
        if: always()
        run: tar --exclude=.git -czf "$RUNNER_TEMP/MathematikAtlas-source.tar.gz" .
      - name: Quellbaum als Artefakt bereitstellen
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: MathematikAtlas-source
          path: ${{ runner.temp }}/MathematikAtlas-source.tar.gz
          if-no-files-found: error
          retention-days: 1
