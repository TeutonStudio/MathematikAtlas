name: Einmalige Knoten-UI-Reparatur

on:
  push:
    branches:
      - agent/fix-node-ui-289-287
    paths:
      - .github/workflows/agent-node-ui-fix.yml

permissions:
  contents: write

jobs:
  reparieren:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Android SDK 36 installieren
        run: sdkmanager "platforms;android-36" "build-tools;35.0.0"
      - name: Knoteninteraktion und Definitionskarten reparieren
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def ersetzen(pfad: str, alt: str, neu: str) -> None:
              datei = Path(pfad)
              text = datei.read_text(encoding="utf-8")
              anzahl = text.count(alt)
              if anzahl != 1:
                  raise SystemExit(f"{pfad}: erwartete genau einen Treffer, gefunden: {anzahl}")
              datei.write_text(text.replace(alt, neu), encoding="utf-8")

          editor = "KnotenKartenVerwalter/src/main/kotlin/de/TeutonStudio/KnotenKartenVerwalter/schnittstelle/KnotenKartenEditor.kt"
          ersetzen(
              editor,
              "import androidx.compose.ui.platform.LocalDensity\n",
              "import androidx.compose.ui.platform.LocalDensity\nimport androidx.compose.ui.res.painterResource\n",
          )
          ersetzen(
              editor,
              "import de.TeutonStudio.KnotenKartenVerwalter.daten.*\n",
              "import de.TeutonStudio.KnotenKartenVerwalter.R\nimport de.TeutonStudio.KnotenKartenVerwalter.daten.*\n",
          )
          ersetzen(
              editor,
              '''        if (zeigeKnotenInspektor) {
            IconButton(
                onClick = {
                    zustand.wähleKnoten(knoten.id)
                    beiInspektorÖffnen()
                },
                modifier = Modifier
                    .align(Alignment.TopEnd)
                    .size(40.dp)
                    .semantics { contentDescription = "Inspektor öffnen" },
            ) {
                Text("⚙", style = MaterialTheme.typography.titleMedium)
            }
        }
''',
              '''        if (zeigeKnotenInspektor) {
            KnotenInspektorSchaltfläche(
                beiKlick = {
                    zustand.wähleKnoten(knoten.id)
                    beiInspektorÖffnen()
                },
                modifier = Modifier.align(Alignment.TopEnd),
            )
        }
''',
          )
          marker = "\n@Composable\nprivate fun BoxScope.AnschlussGriff(\n"
          ergänzung = '''

@Composable
private fun KnotenInspektorSchaltfläche(
    beiKlick: () -> Unit,
    modifier: Modifier = Modifier,
) {
    FilledIconButton(
        onClick = beiKlick,
        modifier = modifier
            .size(40.dp)
            .semantics { contentDescription = "Inspektor öffnen" },
        colors = IconButtonDefaults.filledIconButtonColors(
            containerColor = MaterialTheme.colorScheme.primaryContainer,
            contentColor = MaterialTheme.colorScheme.onPrimaryContainer,
        ),
    ) {
        Icon(
            painter = painterResource(R.drawable.ic_knoten_inspektor),
            contentDescription = null,
            modifier = Modifier.size(20.dp),
        )
    }
}
'''
          ersetzen(editor, marker, ergänzung + marker)

          res = Path("KnotenKartenVerwalter/src/main/res/drawable/ic_knoten_inspektor.xml")
          res.parent.mkdir(parents=True, exist_ok=True)
          res.write_text('''<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FF000000"
        android:pathData="M19.43,12.98c0.04,-0.32 0.07,-0.65 0.07,-0.98s-0.02,-0.66 -0.07,-0.98l2.11,-1.65c0.19,-0.15 0.24,-0.42 0.12,-0.64l-2,-3.46c-0.12,-0.22 -0.37,-0.31 -0.6,-0.22l-2.49,1c-0.52,-0.4 -1.08,-0.73 -1.69,-0.98L14.5,2.42C14.47,2.18 14.25,2 14,2h-4c-0.25,0 -0.46,0.18 -0.49,0.42L9.13,5.07c-0.61,0.25 -1.18,0.59 -1.69,0.98l-2.49,-1c-0.23,-0.08 -0.48,0 -0.6,0.22l-2,3.46c-0.13,0.22 -0.07,0.49 0.12,0.64l2.11,1.65c-0.04,0.32 -0.08,0.66 -0.08,0.98s0.03,0.66 0.08,0.98l-2.11,1.65c-0.19,0.15 -0.24,0.42 -0.12,0.64l2,3.46c0.12,0.22 0.37,0.31 0.6,0.22l2.49,-1c0.52,0.4 1.08,0.73 1.69,0.98l0.38,2.65c0.03,0.24 0.24,0.42 0.49,0.42h4c0.25,0 0.46,-0.18 0.49,-0.42l0.38,-2.65c0.61,-0.25 1.18,-0.58 1.69,-0.98l2.49,1c0.23,0.08 0.48,0 0.6,-0.22l2,-3.46c0.12,-0.22 0.07,-0.49 -0.12,-0.64zM12,15.5A3.5,3.5 0,1 1,12,8a3.5,3.5 0,0 1,0,7.5z" />
</vector>
''', encoding="utf-8")

          renderer = "MathematikKnoten/src/main/kotlin/de/TeutonStudio/MathematikKnoten/MathematikKnotenRenderer.kt"
          ersetzen(
              renderer,
              "    override val interaktionsModus: KnotenInteraktionsModus = KnotenInteraktionsModus.NurKopfzeileZiehbar\n",
              "    override val interaktionsModus: KnotenInteraktionsModus = KnotenInteraktionsModus.GanzeFlächeZiehbar\n",
          )

          konzept = "app/src/main/kotlin/de/TeutonStudio/MathematikAtlas/konzepte/KonzeptUi.kt"
          ersetzen(
              konzept,
              "import androidx.compose.ui.platform.LocalContext\n",
              "import androidx.compose.ui.platform.LocalContext\nimport androidx.compose.ui.text.TextStyle\n",
          )
          ersetzen(
              konzept,
              "import de.TeutonStudio.MathematikKnoten.LatexText\n",
              "import de.TeutonStudio.MathematikKnoten.LatexFormel\nimport de.TeutonStudio.MathematikKnoten.LatexText\n",
          )
          ersetzen(
              konzept,
              "internal object KonzeptDokumentationsRenderer : KnotenRenderer {\n",
              '''internal enum class DokumentationsInhaltArt {
    TEXT,
    INLINE_LATEX,
    DISPLAY_LATEX,
}

private val dokumentationsLatexBefehl = Regex("""\\\\(?:[A-Za-z]+|.)""")

internal fun dokumentationsInhaltArt(
    knoten: KnotenDaten,
    schlüssel: String,
    latexStandard: DokumentationsInhaltArt,
): DokumentationsInhaltArt {
    val ausdrücklich = knoten.parameter["${schlüssel}InhaltArt"]
        ?.trim()
        ?.uppercase()
        ?.replace('-', '_')
    if (ausdrücklich != null) {
        return DokumentationsInhaltArt.entries.firstOrNull { it.name == ausdrücklich }
            ?: DokumentationsInhaltArt.TEXT
    }
    val inhalt = knoten.parameter[schlüssel].orEmpty()
    return if (dokumentationsLatexBefehl.containsMatchIn(inhalt)) latexStandard else DokumentationsInhaltArt.TEXT
}

@Composable
private fun DokumentationsInhalt(
    inhalt: String,
    art: DokumentationsInhaltArt,
    style: TextStyle,
) {
    when (art) {
        DokumentationsInhaltArt.TEXT -> Text(inhalt, style = style)
        DokumentationsInhaltArt.INLINE_LATEX -> LatexText(inhalt, style = style)
        DokumentationsInhaltArt.DISPLAY_LATEX -> LatexFormel(inhalt, style = style)
    }
}

internal object KonzeptDokumentationsRenderer : KnotenRenderer {
''',
          )
          ersetzen(
              konzept,
              '''                    Text(knoten.parameter["regel"].orEmpty(), style = MaterialTheme.typography.bodyMedium)
''',
              '''                    DokumentationsInhalt(
                        inhalt = knoten.parameter["regel"].orEmpty(),
                        art = dokumentationsInhaltArt(
                            knoten = knoten,
                            schlüssel = "regel",
                            latexStandard = DokumentationsInhaltArt.DISPLAY_LATEX,
                        ),
                        style = MaterialTheme.typography.bodyMedium,
                    )
''',
          )
          typ_alt = '''                    Text(knoten.parameter["typ"].orEmpty(), style = MaterialTheme.typography.bodySmall)
'''
          typ_neu = '''                    DokumentationsInhalt(
                        inhalt = knoten.parameter["typ"].orEmpty(),
                        art = dokumentationsInhaltArt(
                            knoten = knoten,
                            schlüssel = "typ",
                            latexStandard = DokumentationsInhaltArt.INLINE_LATEX,
                        ),
                        style = MaterialTheme.typography.bodySmall,
                    )
'''
          text = Path(konzept).read_text(encoding="utf-8")
          if text.count(typ_alt) != 2:
              raise SystemExit(f"{konzept}: erwartete zwei Typ-Textstellen, gefunden: {text.count(typ_alt)}")
          Path(konzept).write_text(text.replace(typ_alt, typ_neu), encoding="utf-8")

          test_drag = Path("MathematikKnoten/src/test/kotlin/de/TeutonStudio/MathematikKnoten/MathematikKnotenRendererInteraktionTest.kt")
          test_drag.parent.mkdir(parents=True, exist_ok=True)
          test_drag.write_text('''package de.TeutonStudio.MathematikKnoten

import de.TeutonStudio.KnotenKartenVerwalter.schnittstelle.KnotenInteraktionsModus
import kotlin.test.Test
import kotlin.test.assertEquals

class MathematikKnotenRendererInteraktionTest {
    @Test
    fun `mathematikknoten lassen sich ueber den gesamten inhalt verschieben`() {
        assertEquals(
            KnotenInteraktionsModus.GanzeFlächeZiehbar,
            MathematikKnotenRenderer().interaktionsModus,
        )
    }
}
''', encoding="utf-8")

          test_latex = Path("app/src/test/kotlin/de/TeutonStudio/MathematikAtlas/DokumentationsInhaltArtTest.kt")
          test_latex.parent.mkdir(parents=True, exist_ok=True)
          test_latex.write_text('''package de.TeutonStudio.MathematikAtlas

import de.TeutonStudio.KnotenKartenVerwalter.daten.KnotenDaten
import kotlin.test.Test
import kotlin.test.assertEquals

class DokumentationsInhaltArtTest {
    @Test
    fun `mathbb typ wird als inline latex erkannt`() {
        val knoten = KnotenDaten(
            art = KonzeptKnotenArten.EINGANG,
            name = "Wertevorrat",
            parameter = mapOf("typ" to "A_{\\\\mathbb{N}} \\subseteq \\mathbb{R}"),
        )

        assertEquals(
            DokumentationsInhaltArt.INLINE_LATEX,
            dokumentationsInhaltArt(knoten, "typ", DokumentationsInhaltArt.INLINE_LATEX),
        )
    }

    @Test
    fun `expliziter text bleibt trotz latex aehnlicher zeichen text`() {
        val knoten = KnotenDaten(
            art = KonzeptKnotenArten.REGEL,
            name = "Hinweis",
            parameter = mapOf(
                "regel" to "Der Pfad \\\\mathbb ist hier nur Dokumentation.",
                "regelInhaltArt" to "text",
            ),
        )

        assertEquals(
            DokumentationsInhaltArt.TEXT,
            dokumentationsInhaltArt(knoten, "regel", DokumentationsInhaltArt.DISPLAY_LATEX),
        )
    }
}
''', encoding="utf-8")

          Path(".github/workflows/agent-node-ui-fix.yml").unlink()
          PY
      - name: Architektur prüfen
        run: python3 scripts/pruefe_architektur.py
      - name: Bauen und testen
        run: ./gradlew --stacktrace test :app:assembleDebug
      - name: Reparatur committen
        shell: bash
        run: |
          git config user.name "OpenAI Codex"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Knoteninteraktion und Definitionskarten reparieren" -m "- erlaubt das Verschieben von Mathematikknoten über den gesamten Knoteninhalt\n- ersetzt das Unicode-Zahnrad durch ein kontrastadaptives VectorDrawable\n- modelliert Dokumentationsinhalt als Text, Inline-LaTeX oder Display-LaTeX\n- rendert mathbb-Ausdrücke in Definitionskarten über den gemeinsamen LaTeX-Kern\n- ergänzt Regressionstests für Drag-Modus und Inhaltsarten\n\nRefs #276 #279 #280\nCloses #287\nCloses #289"
          git push origin HEAD:agent/fix-node-ui-289-287
