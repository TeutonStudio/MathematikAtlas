# Abgeschlossene ExecPlans

Hier liegen vollständig implementierte und durch `node_verifier` abgenommene ExecPlans.

Ein Plan wird nicht allein deshalb verschoben, weil der Implementierer aufgehört hat. Auch Software besitzt gelegentlich einen Unterschied zwischen „fertig“ und „niemand arbeitet gerade daran“.
